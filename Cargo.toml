[workspace]
    members  = ["client", "server", "shared"]
    resolver = "3"

    [workspace.dependencies]
        shared     = { path = "shared" }
        serde      = { version = "*", features = ["std", "derive"] }
        serde_json = { version = "*" }
