[workspace]
    members  = ["client", "server", "shared"]
    resolver = "3"

    [workspace.dependencies]
        strum  = { version = "0.27", features = ["derive"] }
        shared = { path = "shared" }
