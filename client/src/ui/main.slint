export component MainWindow inherits Window {
    VerticalLayout {
        Text {
            text: "Hello, world";
        }

        Image {
            source: @image-url("@images/pinecone.jpg");
        }
    }
}
