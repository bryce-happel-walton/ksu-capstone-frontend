[package]
    name    = "shared"
    version = "0.1.0"
    edition = "2024"

[lib]
    path = "src/lib.rs"
    name = "shared"

[dependencies]
    strum = { version = "*", features = ["derive"] }

[build-dependencies]
    bindgen = "*"
