[package]
    name    = "shared"
    version = "0.3.0"
    edition = "2024"

[lib]
    path = "src/lib.rs"
    name = "shared"

[dependencies]
    strum                       = { workspace = true }
    struct-field-names-as-array = "*"

[build-dependencies]
    bindgen = "*"
