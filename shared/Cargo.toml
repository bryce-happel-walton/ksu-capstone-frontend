[package]
    name    = "shared"
    version = "1.0.0"
    edition = "2024"

[lib]
    path = "src/lib.rs"
    name = "shared"

[dependencies]
    struct-field-names-as-array = "0.3.0"
    strum                       = { version = "0.28.0", features = ["derive"] }

[build-dependencies]
    bindgen = "0.72.1"
